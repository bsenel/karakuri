# Karakuri Workflows

Workflow YAML files define role hints, constraints, and checkpoints for each operational mode.

Add custom workflows to this directory and register them in `config/default.yaml`.
